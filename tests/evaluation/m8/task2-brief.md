# M8 Task 2 fixed brief (2.0)

Baseline: upstream commit `f659f1d8c86ad39925eda44b55d569b175e96e57`, plus local seed commit `26ca1d6ad9ee274c5f5680c226c53fd72c55d986`. The seed retains the M4 employee search, Grid/List, selection, salary editing, paging, and save flow, but removes the detail email control and application validator, and makes save send every row. The fixed acceptance is `tests/browser/m8-task2.spec.mjs` with `tests/playwright.eval.config.mjs`; do not edit either file.

Add an editable email field to the selected-detail Form in both existing authored M4 layouts. It must be required, use the built-in `email` validator and one application-defined `companyEmail` validator through declarative Form dispatch, and show `Use a company email.` for an address outside `example.com`. Invalid syntax and a valid-format wrong-domain address must each send zero save requests. After editing Ada's salary and entering a valid company address, save only changed rows with their actual change status and raw values. Keep the other M4 features working. Use public framework package exports, not internal modules. Preserve the authored layouts and their distinct arrangements.

Acceptance, in this order: `npm run build`, `npm run typecheck`, `node <root>/node_modules/typescript/bin/tsc --ignoreConfig --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --lib ES2022,DOM,DOM.Iterable --strict --skipLibCheck examples/vite/m4/employees.ts`, `npx playwright test -c tests/playwright.eval.config.mjs tests/browser/m8-task2.spec.mjs --project=chromium --workers=1`, and `npm run docs:check -- --changed` after documenting source changes. The Chromium test has side and stack cases. It checks the required declarative email rule pair, zero requests for both invalid inputs, a single changed-row update payload with raw salary, Saved status, and zero page errors. Keep task-specific documentation in the English OKF concept, index, and log. Do not edit the fixed acceptance test or framework source.

For formal measurement, use a fresh `gpt-6-sol` agent with `fork_turns:none`, this brief, and no earlier M8 task history. Only content reads/searches/diff reviews through `node <evaluation-base>/meter.mjs --work <isolated-checkout> {read|read-lines|search|files|review} ...`; pass a tool output budget large enough for complete output. The meter records UTF-8 emitted bytes and content-file paths. Build/test output is separate. Use the same method for Tasks 1 and 3. A full output that the tool truncates invalidates the byte measurement; split it and rerun. Count the first complete acceptance before any corrective edit, then record code-change retries and environment-only reruns separately. Stop timing at the first fully passing run. Use `git add -N` for new files, then `git diff --numstat` against the seed. Actual tokens are reported only if telemetry is exposed. A failed check is a correctness failure, not hidden by later repair.

The evaluation checkout lives beneath `node_modules/.cache`, where Node refuses to strip types from a TypeScript Playwright config or test. The frozen `.mjs` config and test target the same browser behavior and Vite server with `reuseExistingServer: false`. This is an environment adapter, not application code. Coordinate port 4173 with the other evaluations before browser tests.

The baseline archive SHA-256 is `4F0AF4527034494F737D1563E24CD891B30A5C2DB40EBF28A2A537F50DF66191`. The fixed seed overlay archive SHA-256 is `BA39CC15DD2EAA6294D19A25537C987C113DF3B5F0AC222A7E15DC233A484990`. `baseline.tar` contains the pinned upstream tree; `task2-seed.tar` contains the five seed files. The meter writes `<isolated-checkout>-meter.jsonl` beside the checkout when `--work` is supplied, keeps search paths even for one-file searches, and refuses output above 24 KiB. Use `read-lines` or a narrower search for larger output.

Seed check: `npm run build`, `npm run typecheck`, and the direct M4 TypeScript check pass. The fixed Chromium test fails both layout cases at the missing detail email control (`tests/browser/m8-task2.spec.mjs:24`, expected 1, received 0). A separate validation checkout proved both cases can pass without editing the fixed acceptance. The seed and formal work checkout remain unchanged.
