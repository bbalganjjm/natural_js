# M8 Task 1 fixed brief (2.0 pilot and formal replay)

Baseline: upstream commit f659f1d8c86ad39925eda44b55d569b175e96e57, plus local seed commit 2e9d00b12dec3b4bd99bdcde515c2e6bcb9fea0a. The seed adds authored `examples/vite/m8-task1/{side,stack}.html` and matching CSS, `employees.json`, and `tests/browser/m8-task1.spec.mjs`, and `tests/playwright.eval.config.mjs`. It intentionally has no `main.ts`; do not copy or alias the complete M4 employee controller.

Implement one CVC controller and a small screen opener in `examples/vite/m8-task1/main.ts`. It must attach behavior to both existing authored layouts without changing their HTML/CSS, load the fixed employee JSON, bind the search Form, read-only List with nested row-local Select, and selected detail Form to one caller-owned Rows store per screen. Search by employee name, ignoring case. Native Enter submits search; Enter on a list selection button selects that row; selected details remain independent across two screens. The selected detail Form can edit `profile.team`; the List should reflect it. Open another screen and close a screen without leaking its page work. Keep selectors scoped and DOM IDs unique. Use framework package exports, not internal modules. The seed test is fixed and must not be edited.

Acceptance, in this order: `npm run build`, `npm run typecheck`, `node <root>/node_modules/typescript/bin/tsc --ignoreConfig --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --lib ES2022,DOM,DOM.Iterable --strict --skipLibCheck examples/vite/m8-task1/main.ts`, `npx playwright test -c tests/playwright.eval.config.mjs tests/browser/m8-task1.spec.mjs --project=chromium`, and `npm run docs:check -- --changed` after documenting the example. The Playwright test has two layout cases and checks keyboard search/selection, nested Select raw value 22 and skipped incomplete option, detail edit propagation, two independent screen instances, cleanup, zero duplicate DOM IDs, and zero page errors. Keep task-specific documentation in an English OKF concept, index, and log. Do not edit the fixed acceptance test or framework source.

For formal measurement, use a fresh `gpt-6-sol` agent with `fork_turns:none`, the same brief, and no M7 task history. Only content reads/searches/diff reviews through `node <evaluation-base>/meter.mjs --work <isolated-checkout> {read|read-lines|search|files|review} ...`; pass a tool output budget large enough for complete output. The meter records UTF-8 emitted bytes and content-file paths. Build/test output is separate. Use the same method for Tasks 2 and 3. A full output that the tool truncates invalidates the byte measurement; split it and rerun. Count the first complete acceptance before any corrective edit, then record code-change retries and environment-only reruns separately. Stop timing at the first fully passing run. Use `git add -N` for new files, then `git diff --numstat` against the seed. Actual tokens are reported only if telemetry is exposed. A failed check is a correctness failure, not hidden by later repair.

The current pilot agent inherited M7 Popup context and is therefore not a fresh-agent cost datapoint. Its role is to validate the seed and acceptance procedure. Do not compare its read bytes or elapsed time with previous milestones or 1.x.

The evaluation checkout lives beneath `node_modules/.cache`, where Node refuses to strip types from a TypeScript Playwright config or test. The frozen `.mjs` config and test targets the same test and Vite server with `reuseExistingServer: false`. This is an environment adapter, not application code.

The fixed seed archive has SHA-256 `8AE5F81F2F58EFF35B9788C3A61B42E8C8B6176B46781F13A00CCCA63BC4052E`. The meter writes `<isolated-checkout>-meter.jsonl` beside the checkout when `--work` is supplied, keeps search paths even for one-file searches, and refuses output above 24 KiB so no tool truncation can silently inflate the recorded read context. Use `read-lines` or a narrower search for larger output.
