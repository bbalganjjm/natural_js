# M8 Task 3 fixed brief (2.0 formal evaluation)

Baseline: upstream post-M7 commit f659f1d8c86ad39925eda44b55d569b175e96e57, extracted into an independent Git repository. The seed commit is 014cee6897a09290a066ffd26daff4385a2b8c5d. It adds one authored profile page, a delayed JSON fixture, and two fixed Chromium acceptance cases. The existing application example deliberately contains a close/reopen race and a retained document listener. Do not edit the fixed acceptance test or framework source.

Repair examples/vite/m8-task3/main.ts so closing the page while its request is pending causes zero late updates to its removed root; a reopened page renders its own response correctly; and repeated close/reopen cycles keep one active refresh listener. Keep the same mountPage CVC runtime, authored HTML, and package exports. Do not build a new page runner, global utility, or registry.

Acceptance, in this order: npm run build; npm run typecheck; node <root>/node_modules/typescript/bin/tsc --ignoreConfig --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --lib ES2022,DOM,DOM.Iterable --strict --skipLibCheck examples/vite/m8-task3/main.ts; npx playwright test -c tests/playwright.eval.config.mjs tests/browser/m8-task3.spec.mjs --project=chromium; npm run docs:check -- --changed. The browser tests must both pass. They hold the first response until after disposal/reopening, inspect the detached root, count refresh requests after eight cycles and after final close, and check for page errors and duplicate IDs. Keep tests/browser/m8-task3.spec.mjs and tests/playwright.eval.config.mjs fixed. Document the actual example in a concise English OKF concept under docs/v2/, link it from docs/v2/index.md and docs/index.md, update docs/log.md, and stamp the concept.

Formal measurement: use a fresh agent without prior M7 task context. Read/search/file-list/diff-review only through node <evaluation-base>/meter.mjs --work <isolated-checkout> {read|search|files|review} ... from the clean work directory; give tool output enough budget to avoid truncation. The meter records UTF-8 output bytes including rereads and unique content paths. Build/test output is separate. Record the first complete acceptance before corrective edits, any code-change retries, unique read files, emitted bytes, changed files and added/deleted lines, elapsed time, and actual tokens only if telemetry exposes them. A failed first check remains a first-pass correctness failure even if repaired. Use git add -N for new files before git diff --numstat. Stop timing at the first full pass. Do not compare these proxy bytes to token usage or to previous tasks with a different method.

The checkout is nested under node_modules/.cache. TypeScript Playwright config/tests cannot run there under Node type stripping, so the seed includes a fixed .mjs Playwright config and .mjs test. Vite uses port 4173 with reuseExistingServer false; do not run another Vite browser suite concurrently. The seed baseline is intentionally red: delayed root receives Profile 1 after close, and one refresh emits eight requests after eight openings.
